# Clinical Grant Workbench
# Recommended real-world defaults for an Apple Silicon M4 MacBook with 24 GB RAM.
#
# This is a template, not a file loaded automatically by the application.
# After reviewing the organization name and credentials, install it with:
#   cp env.m4Mac.txt .env
#
# Keep .env private. Never commit API keys or confidential grant data.

# Organization and document plan
ORGANIZATION_NAME="Research Clinic Cancer Center"
AUTH_MODE=internal_accounts
INITIAL_ADMIN_SETUP_TOKEN=
AUTH_SESSION_TTL_SECONDS=43200
PASSWORD_RESET_TTL_SECONDS=1800
PASSWORD_MIN_LENGTH=14
PASSWORD_MAX_LENGTH=256
LOGIN_MAX_FAILURES=5
LOGIN_LOCK_SECONDS=900
APP_PUBLIC_URL=http://127.0.0.1:7860
SESSION_COOKIE_SECURE=false
TRUSTED_PROXY_IPS=127.0.0.1
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
GRANT_SECTIONS="Specific Aims,Significance,Innovation,Approach,Human Subjects,Environment"
GRANT_WRITER_HOME=./workspace
GRANT_EXPORT_HOME=./exports

# Force the physical M4/24 GB profile. This avoids incorrect selection when a
# sandbox or virtualized shell reports less memory than the Mac actually has.
GRANT_RUNTIME_PROFILE=apple_ollama
MODEL_ROUTING_MODE=local_only
REQUIRE_CLAUDE_IN_HYBRID=false

# Set MODEL_ROUTING_MODE=hybrid and add the key to enable Claude escalation.
# Claude plans online searches and validates the evidence retrieved by the bounded
# backend research connectors; it also handles compliance and high-value synthesis.
# OLMo 3 remains the default local section-drafting model. To use Qwen 3 on
# the same M4, copy env.m4Mac.qwen3.txt instead of this template.
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-4-5
CLAUDE_MAX_TOKENS=16000
CLAUDE_TASK_KINDS=requirement_decomposition,sponsor_compliance_compilation,investigator_interview,research_planning,evidence_validation,competitor_profile,competitive_positioning,complex_scientific_synthesis,review_simulation,causal_analysis

# Native Ollama generation with Apple Metal. LOCAL_LLM_* is the provider-neutral contract;
# OLMO_* is retained while older deployments migrate.
LOCAL_LLM_PROVIDER=ollama
LOCAL_LLM_MODEL_REPO=
LOCAL_LLM_MODEL_REVISION=
LOCAL_LLM_API_MODEL=olmo-3:7b-instruct-q4_K_M
LOCAL_LLM_URL=http://host.docker.internal:11434/v1/chat/completions
LOCAL_LLM_MAX_TOKENS=16000
LOCAL_LLM_CONTEXT_TOKENS=24576
LOCAL_LLM_PROMPT_PREFIX=
OLLAMA_CONTEXT_LENGTH=24576
OLMO_MODEL_REPO=
OLMO_MODEL_REVISION=
OLMO_API_MODEL=olmo-3:7b-instruct-q4_K_M
OLMO_URL=http://host.docker.internal:11434/v1/chat/completions
OLMO_PORT=11434
OLMO_MAX_TOKENS=16000
# Containerized CPU embeddings
CPU_EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
CPU_EMBEDDING_API_MODEL=grant-embedding-cpu
CPU_EMBEDDING_THREADS=2
CPU_EMBEDDING_BATCH_SIZE=4
EMBEDDING_API_MODEL=grant-embedding-cpu
EMBEDDING_URL=http://embedding-cpu:8010/v1/embeddings
EMBEDDING_DOCUMENT_PREFIX="passage: "
EMBEDDING_QUERY_PREFIX="query: "
EMBEDDING_BATCH_SIZE=4
INDEX_EMBED_BATCH_RECORDS=4
EMBEDDING_HTTP_TIMEOUT_SECONDS=180
EMBEDDING_HEALTH_TTL_SECONDS=600
MODEL_HTTP_TIMEOUT_SECONDS=900

# M4 CPU parallelism. Eight worker threads leave two efficiency/performance
# cores available for macOS and Docker Desktop on a 10-core base M4.
# configure_runtime.sh derives matching thread and container CPU ceilings from
# the physical core count and writes the effective values to .runtime.env.
OMP_NUM_THREADS=8
RAYON_NUM_THREADS=8
OPENBLAS_NUM_THREADS=1

# Online research connectors used to execute Claude's research plans. Brave is
# required by the Research tab and patent/technology web enrichment. OpenAlex
# credentials enable publication discovery.
BRAVE_SEARCH_API_KEY=
BRAVE_SEARCH_ENDPOINT=https://api.search.brave.com/res/v1/web/search
OPENALEX_MAILTO=
OPENALEX_API_KEY=
RESEARCH_USER_AGENT=ClinicalGrantWorkbench/0.8.0
RESEARCH_MAX_CONCURRENCY=8
RESEARCH_HTTP_TIMEOUT_SECONDS=45
RESEARCH_MAX_BODY_BYTES=8388608

# Document ingestion and prompt context
DOCUMENT_INGESTION_URL=http://ingestion:8091/extract
INGESTION_HTTP_TIMEOUT_SECONDS=90
INGESTION_NAVIGATION_TIMEOUT_MS=45000
INGESTION_MAX_OUTPUT_BYTES=8388608
INGESTION_MAX_CONCURRENCY=2
DOCUMENT_CHUNK_WORDS=420
DOCUMENT_CHUNK_OVERLAP_WORDS=64
CONTEXT_RETRIEVAL_K=24
CONTEXT_MAX_CHARS=48000

# Hybrid retrieval scoring
RETRIEVAL_WEIGHT_SEMANTIC=0.45
RETRIEVAL_WEIGHT_LEXICAL=0.25
RETRIEVAL_WEIGHT_EVIDENCE=0.20
RETRIEVAL_WEIGHT_FRESHNESS=0.10
RETRIEVAL_GRAPH_BOOST=0.08
RETRIEVAL_CANDIDATE_MULTIPLIER=4
RETRIEVAL_OPENMP_THRESHOLD=4096
RETRIEVAL_FRESHNESS_HALF_LIFE_DAYS=365
BM25_K1=1.2
BM25_B=0.75

# Clinical feasibility safety ceiling
CLINICAL_SCENARIO_MAX_COMBINATIONS=10000

# Public competitive-applicant intelligence
COMPETITIVE_CONFIG_PATH=/app/config/competitive_intelligence.json
WORKFLOW_DEFINITIONS_PATH=/app/config/workflow_definitions.json
COMPETITIVE_HTTP_TIMEOUT_SECONDS=60
COMPETITIVE_REFRESH_TTL_SECONDS=14400
COMPETITIVE_BACKGROUND_REFRESH_ENABLED=true
COMPETITIVE_BACKGROUND_REFRESH_SECONDS=14400
COMPETITIVE_UI_POLL_SECONDS=14400
COLLABORATION_UI_POLL_SECONDS=5
COMPETITIVE_UPDATE_LABEL="Competitive Edge Auto-Update"
COMPETITIVE_USER_AGENT=GrantWriterCompetitive/1.0
COMPETITIVE_HOME_ORGANIZATION_ALIASES="Research Clinic Cancer Center"

# CPU embedding container resource values.
CPU_EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
CPU_EMBEDDING_API_MODEL=grant-embedding-cpu
CPU_EMBEDDING_THREADS=2
CPU_EMBEDDING_BATCH_SIZE=16

# Container resource ceilings appropriate for a 24 GB Mac. CORE_CPU_LIMIT is
# deliberately aligned with OMP_NUM_THREADS/RAYON_NUM_THREADS to prevent HPC
# oversubscription inside Docker.
CORE_MEMORY_LIMIT=2g
UI_MEMORY_LIMIT=768m
RENDERER_MEMORY_LIMIT=1g
INGESTION_MEMORY_LIMIT=1g
EMBEDDING_MEMORY_LIMIT=768m
CORE_CPU_LIMIT=8.0
UI_CPU_LIMIT=1.0
RENDERER_CPU_LIMIT=1.5
INGESTION_CPU_LIMIT=1.5
EMBEDDING_CPU_LIMIT=2.0
OLLAMA_MEMORY_LIMIT=10g
OLLAMA_CPU_LIMIT=8.0

# Build and operations
GRANT_BUILD_VERSION=0.8.0
GRANT_BUILD_REVISION=development
BACKUP_DIR=./backups
BENCHMARK_OUTPUT_DIR=./benchmarks
BENCHMARK_ITERATIONS=3
RELEASE_DIR=./releases
REQUIRE_AUDIT_TOOLS=false
REQUIRE_RELEASE_SIGNATURE=false
